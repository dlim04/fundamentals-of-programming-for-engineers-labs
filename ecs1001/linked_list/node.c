#include "node.h"

#include <stdlib.h>

node *node_create_node(int value) {
  // Implement node_create_node() to match the documentation in node.h
  return NULL;
}

void node_add_node(node **head, node *new_node) {
  // Implement node_add_node() to match the documentation in node.h
  return NULL;
}

int node_pop_head(node **head) {
  // Implement node_pop_head() to match the documentation in node.h
  return NULL;
}
